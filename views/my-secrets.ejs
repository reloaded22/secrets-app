<%- include('partials/header') %>
<%- include('navbar', {loggedIn:loggedIn}) %>

  <div class="container-fluid pt-5 text-center">
    <h1 class="display-3"><i class="fas fa-key fa-sm text-secondary"></i> Here are all my secrets:</h1>
    <hr>
    <div class="row g-2">
      <%let count = -1%>
      <%secrets.forEach((secret, i)=>{%>
        <%count += 1%>
        <% if (count%2 === 0) { %>
          <div class="col-4 col-md-3 col-lg-2 text-capitalize text-danger">
            <%- include('my-cards', {secret:secret, index:i}) %>
          </div>
        <%} else {%>
          <div class="col-4 col-md-3 col-lg-2 text-capitalize text-info">
            <%- include('my-cards', {secret:secret, index:i}) %>
          </div>
        <%}%>
      <%})%>
    </div>
  </div>
  <hr>
  <div class="text-center">
    <a class="btn btn-dark" href="/submit" role="button">Submit a Secret</a>
  </div>
</div>

<%- include('partials/footer') %>
